{
  "service": "DFP 2.0 search worker",
  "version": "v78",
  "release_date": "2026-08-01",
  "railway_ready": true,
  "module_version": "karnataka_recovery_v5_2_password_free_routes",
  "features": [
    "fail-closed NGO website ownership verification",
    "hard carrier, directory, fundraiser, vendor, article, travel, social and reference rejection",
    "independent site-root ownership crosscheck for every deep or query URL",
    "current-rule revalidation of every retained or historical URL regardless of its old label",
    "known official fetch failure preservation instead of directory replacement",
    "explicit parent/project and controlled hosted-microsite verification",
    "three-layer safety enforcement: candidate verifier, final result guard and deterministic Avika-export rebuild",
    "one SERPER_API_KEY only",
    "permanent NGO IDs and immutable source-record IDs",
    "resumable checkpoints and optional selective Firecrawl",
    "status endpoint hotfix: persisted ok field no longer causes duplicate-keyword 500",
    "Karnataka Recovery start, pause, cancel and resume are password-free"
  ],
  "tests": {
    "pytest": "98 passed",
    "python_compile": "passed",
    "built_in_ownership_guard": "18 deterministic cases passed"
  },
  "notes": [
    "Karnataka Recovery does not require ADMIN_PASSWORD. The frontend proxy may call it without credentials.",
    "Unrelated consequential legacy endpoints remain protected when ADMIN_PASSWORD is configured.",
    "Use SERPER_CONCURRENCY=4 initially. SERPER_API_KEYS is ignored.",
    "Use Frontend v160 or newer; no frontend redeployment is required for this worker-only hotfix."
  ]
}
