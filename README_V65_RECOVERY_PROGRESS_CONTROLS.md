# Railway Worker v65 — Recovery Progress Controls

Adds pause, safe stop, checkpoint resume, live counts and rate-based ETA to the Serper and selective Firecrawl website-recovery jobs. Output rows are checkpointed by append rather than rewriting the growing audit on every NGO, making large runs safer and more scalable.
