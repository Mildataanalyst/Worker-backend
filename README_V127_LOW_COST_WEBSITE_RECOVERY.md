# Frontend v127 — Low-cost website recovery controls

- Advanced website recovery now states that it uses at most two Serper searches.
- Brave and Firecrawl are shown as disabled by default.
- Added **Avika-filter input** download.
- Added **Retry unresolved/fetch errors** download.
- `candidate_site_unreachable` contributes to the incomplete count.
- Normal repository runs show **Retry fetch/search errors** when retryable rows
  remain, even when other outputs are already available.
